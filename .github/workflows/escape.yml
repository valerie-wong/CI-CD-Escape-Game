name: DevOps Escape Room
on: [push]

jobs:
  build_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
    jobs:
      - name: Confirm actions are enabled
        run: echo "GitHub Actions initialized for this fork."
